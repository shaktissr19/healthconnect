# Legacy frontend maintenance scripts

This directory contains archived one-off patch scripts used during earlier HealthConnect UI development.

They are **not part of the Next.js runtime, build, or deployment flow**. The current application source already contains the resulting UI changes, so these scripts are retained only for historical/reference purposes.

Do not use them as a normal way to modify current frontend code.
